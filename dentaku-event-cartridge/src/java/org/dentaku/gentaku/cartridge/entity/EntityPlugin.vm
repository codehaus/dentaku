/**
 * This class is only generated once! It will never be overwritten.
 * You can (and have to!) safely modify it by hand.
 */
package ${plugin.getDestinationPackage($metadata)};

/**
  * @hibernate.class table="${metadata.name}"
  */
public class ${metadata.name} extends ${metadata.name}Base {

}
